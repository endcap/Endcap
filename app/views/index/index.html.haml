#featuredcontent
  .wrapper
    %div{:id => "featuredartist"}
      #video
        <iframe src="http://player.vimeo.com/video/16874071" width="500" height="375" frameborder="0"></iframe>
    %div{:id => "discover"}
      %h2 Discover Local Music
      %p What artists and bands are in your local area?  What band is that one musician in, now? Endcap can let you know. Explore local events, venues, bands, and music, and check out what’s going on in your local music scene.  Sounds cool?  Awesome!
      %p{:class => "calltoaction"} Check it out >>          

#maincontent
  .wrapper
    #bandlist
      %h2 Bands
      %ul
        - @bands.each do |band|
          %li{:style => "height: 45px"}
            =link_to band.name, band
            -if band.image and band.image.length > 0
              %img{:src => band.image, :style => "width: 50px; height:50px; float: right" }
            
    #eventlist
      %h2 Events
      %ul
        - @events.each do |event|
          %li{:class => "event", :style => "height: 45px"}
            %span{:style => "float: left"}
              %p{:class => "artist"}
                =link_to event.name, event
              %p{:class => "show"}
                = event.time
                @
                =link_to event.venue.name, event.venue
            -if event.venue.image and event.venue.image.length > 0
              %img{:src => event.venue.image, :style => "width: 50px; height:50px; float: right" }
    #songlist
      %h2 Songs
      %ul
        - @songs.each do |song|
          %li{:class => "song"}
            %p{:class => "title"}
              =link_to "\"#{song.name}\"", song
            %p{:class => "artist"}
              - if song.album and song.album.band
                = link_to song.album.band.name, song.album.band
%script{:type => "text/javascript"}
  mpq.push(["track", "Load home page"]);