%p#notice= notice

- if @album.image
  %img{ :src => @album.image, :align => "right" }

%h3
  = @album.name
%p
  %b Band:
  
  = link_to @album.band.name, @album.band
%p
  %b Year:
  = @album.year
%p
  %b Description:
  = @album.description

%h3 Songs
%ol
  - @album.songs.each do |song|
    = song.name
    
<a href="/songs/new/?id=#{@album.id}">Edit Songs</a>
%br

= link_to 'Edit', edit_album_path(@album)
\|
= link_to 'Back', albums_path
